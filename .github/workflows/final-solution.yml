name: FINAL SOLUTION - Create Gradle and Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: ⚙️ Setup Environment
      uses: android-actions/setup-android@v3
      
    - name: 🛠️ Create Complete Gradle Structure
      run: |
        echo "=== СОЗДАЕМ ПОЛНУЮ GRADLE СТРУКТУРУ ==="
        cd android-app
        
        # Создаем gradlew файл
        echo "📝 Создаем gradlew..."
        cat > gradlew << 'EOF'
#!/usr/bin/env sh

##############################################################################
##
##  Gradle start up script for UN*X
##
##############################################################################

# Attempt to set APP_HOME
# Resolve links: $0 may be a link
PRG="$0"
# Need this for relative symlinks.
while [ -h "$PRG" ] ; do
    ls=`ls -ld "$PRG"`
    link=`expr "$ls" : '.*-> \(.*\)$'`
    if expr "$link" : '/.*' > /dev/null; then
        PRG="$link"
    else
        PRG=`dirname "$PRG"`"/$link"
    fi
done
SAVED="`pwd`"
cd "`dirname \"$PRG\"`/" >/dev/null
APP_HOME="`pwd -P`"
cd "$SAVED" >/dev/null

APP_NAME="Gradle"
APP_BASE_NAME=`basename "$0"`

# Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to this script.
DEFAULT_JVM_OPTS='"-Xmx64m" "-Xms64m"'

# Use the maximum available, or set MAX_FD != -1 to use that value.
MAX_FD="maximum"

warn () {
    echo "$*"
}

die () {
    echo
    echo "$*"
    echo
    exit 1
}

# OS specific support (must be 'true' or 'false').
cygwin=false
msys=false
darwin=false
nonstop=false
case "`uname`" in
  CYGWIN* )
    cygwin=true
    ;;
  Darwin* )
    darwin=true
    ;;
  MINGW* )
    msys=true
    ;;
  NONSTOP* )
    nonstop=true
    ;;
esac

CLASSPATH=$APP_HOME/gradle/wrapper/gradle-wrapper.jar

# Determine the Java command to use to start the JVM.
if [ -n "$JAVA_HOME" ] ; then
    if [ -x "$JAVA_HOME/jre/bin/java" ] ; then
        # IBM's JDK on AIX uses strange locations for the executables
        JAVACMD="$JAVA_HOME/jre/bin/java"
    else
        JAVACMD="$JAVA_HOME/bin/java"
    fi
    if [ ! -x "$JAVACMD" ] ; then
        die "ERROR: JAVA_HOME is set to an invalid directory: $JAVA_HOME"
    fi
else
    JAVACMD="java"
    which java >/dev/null 2>&1 || die "ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH."
fi

# Increase the maximum file descriptors if we can.
if [ "$cygwin" = "false" ] && [ "$msys" = "false" ] && [ "$nonstop" = "false" ] ; then
    MAX_FD_LIMIT=`ulimit -H -n`
    if [ $? -eq 0 ] ; then
        if [ "$MAX_FD" = "maximum" ] || [ "$MAX_FD" = "max" ] ; then
            MAX_FD="$MAX_FD_LIMIT"
        fi
        ulimit -n $MAX_FD
        if [ $? -ne 0 ] ; then
            warn "Could not set maximum file descriptor limit: $MAX_FD"
        fi
    else
        warn "Could not query maximum file descriptor limit: $MAX_FD_LIMIT"
    fi
fi

# For Darwin, add options to specify how the application appears in the dock
if $darwin; then
    GRADLE_OPTS="$GRADLE_OPTS \"-Xdock:name=$APP_NAME\" \"-Xdock:icon=$APP_HOME/media/gradle.ico\""
fi

# For Cygwin or MSYS, switch paths to Windows format before running java
if [ "$cygwin" = "true" ] || [ "$msys" = "true" ] ; then
    APP_HOME=`cygpath --path --mixed "$APP_HOME"`
    CLASSPATH=`cygpath --path --mixed "$CLASSPATH"`
    JAVACMD=`cygpath --unix "$JAVACMD"`

    # We build the pattern for arguments to be converted via cygpath
    ROOTDIRSRAW=`find -L / -maxdepth 1 -mindepth 1 -type d 2>/dev/null`
    SEP=""
    for dir in $ROOTDIRSRAW ; do
        ROOTDIRS="$ROOTDIRS$SEP$dir"
        SEP="|"
    done
    OURCYGPATTERN="(^($ROOTDIRS))"
    # Add a user-defined pattern to the cygpath arguments
    if [ "$GRADLE_CYGPATTERN" != "" ] ; then
        OURCYGPATTERN="$OURCYGPATTERN|($GRADLE_CYGPATTERN)"
    fi
    # Now convert the arguments
    for i do
        EQUALS=`echo $i | cut -d = -f 1-`
        if echo $EQUALS | eval grep -E -q -- "$OURCYGPATTERN" ; then
            eval cygpath --path --ignore --windows "$i"
        else
            echo $i
        fi
    done
fi

# Split up the JVM_OPTS into GRADLE_OPTS and JAVA_OPTS
JAVA_OPTS="$JAVA_OPTS $DEFAULT_JVM_OPTS"

exec "$JAVACMD" $JAVA_OPTS $GRADLE_OPTS -classpath "$CLASSPATH" org.gradle.wrapper.GradleWrapperMain "$@"
EOF

        chmod +x gradlew
        echo "✅ gradlew создан"

        # Создаем папку gradle/wrapper
        mkdir -p gradle/wrapper
        
        # Создаем gradle-wrapper.properties
        echo "📝 Создаем gradle-wrapper.properties..."
        cat > gradle/wrapper/gradle-wrapper.properties << 'EOF'
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-7.5-all.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
EOF

        # Создаем пустой gradle-wrapper.jar (символическая ссылка)
        touch gradle/wrapper/gradle-wrapper.jar
        
        echo "✅ Gradle структура создана"
        
    - name: 🔧 Fix Project Configuration
      run: |
        cd android-app
        echo "=== ПРОВЕРЯЕМ И ИСПРАВЛЯЕМ КОНФИГУРАЦИЮ ==="
        
        # Проверяем settings.gradle
        echo "🔍 Проверяем settings.gradle..."
        if grep -q "FALL_ON_PROJECT_REPOS" settings.gradle; then
          echo "🔄 Исправляем опечатку в settings.gradle..."
          sed -i 's/FALL_ON_PROJECT_REPOS/FAIL_ON_PROJECT_REPOS/g' settings.gradle
        fi
        
        # Проверяем наличие app/build.gradle
        if [ ! -f "app/build.gradle" ]; then
          echo "📝 Создаем app/build.gradle..."
          cat > app/build.gradle << 'EOF'
plugins {
    id 'com.android.application'
}

android {
    compileSdk 33

    defaultConfig {
        applicationId "com.example.app"
        minSdk 21
        targetSdk 33
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            debuggable true
        }
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.8.0'
}
EOF
        fi
        
    - name: 🏗️ Build APK
      run: |
        cd android-app
        echo "=== ЗАПУСКАЕМ СБОРКУ ==="
        ./gradlew clean assembleDebug --stacktrace
        
    - name: 📋 Find APK
      run: |
        echo "=== ПОИСК APK ФАЙЛОВ ==="
        find android-app -name "*.apk" -type f | head -10
        
    - name: ⬆️ Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: final-apk
        path: |
          android-app/app/build/outputs/apk/**/*.apk
          android-app/**/*.apk
        if-no-files-found: warn
        retention-days: 7
